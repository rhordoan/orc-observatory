apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orc-experiment-results
  labels:
    app: orc-experiments
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
