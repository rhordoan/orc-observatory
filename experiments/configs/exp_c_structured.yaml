# Experiment C: TSPLIB + QAPLIB structured instances
experiments:
  - escape
  - otg_lon
  - shuffle
use_gpu: true
gamma: 1.0
strategies: [orc, mingap, random]
n_shuffles: 5
optima_mode: sample
grid:
  items:
    - {type: tsplib, instance: eil51, n_restarts: 300, seeds: [0,1,2]}
    - {type: tsplib, instance: berlin52, n_restarts: 300, seeds: [0,1,2]}
    - {type: tsplib, instance: kroA100, n_restarts: 200, seeds: [0,1,2]}
    - {type: qaplib, instance: nug12, n_restarts: 300, seeds: [0,1,2]}
    - {type: qaplib, instance: chr15a, n_restarts: 300, seeds: [0,1,2]}
    - {type: qaplib, instance: rou20, n_restarts: 300, seeds: [0,1,2]}
